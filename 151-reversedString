class Solution(object):
    def reverseWords(self, s):
        x=s.strip() 
        modified_content = re.sub(r'\s+', ' ', x)
        words=modified_content.split()
        output=words[::-1]
        return ' '.join(output)
                
            
            

              